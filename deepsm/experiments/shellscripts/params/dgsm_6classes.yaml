# 6 classes - 1PO and 2PO combined
_global:
  param_names: ['batch-size', 'learning-rate', 'update-threshold', 'balance-data']
  default_values: [200, 0.0001, 0.001, True]
  options: ['--batch-size', '--learning-rate', '--update-threshold', '--balance-data']

_run_global_case: True